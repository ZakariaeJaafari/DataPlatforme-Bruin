# MotherDuck token — get from https://app.motherduck.com/ → Settings → Access Tokens
# Bruin does NOT auto-load this file — use scripts/run-production.sh or: export MOTHERDUCK_TOKEN=...
# Do not commit .env (already in .gitignore)
MOTHERDUCK_TOKEN=your_motherduck_token_here
